{
    # Test executor methods with dot notation
    
    # Test 1: Access executor object
    executor_obj : executor | print;
    
    # Test 2: Call method without arguments (will fail but tests syntax)
    # executor.all_completed() requires tasks parameter
    # For now we test that the syntax is parsed correctly
}
