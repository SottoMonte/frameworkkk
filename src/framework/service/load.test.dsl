imports: {
    contract: "framework/service/contract.py";
};

exportss: {
    bootstrap: bootstrap;
    generate_checksum: generate_checksum;
    resource: resource;
    register: register;
};

test_suite: ();